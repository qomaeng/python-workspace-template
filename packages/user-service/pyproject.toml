[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[project]
name = "user-service"
version = "0.0.1"
description = "Internal user service"
authors = [{ name = "qomaeng", email = "qomaeng@gmail.com" }]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "core>=0.0.1",
]

[tool.uv.build-backend]
module-root = "src"

[tool.pyright]
include = ["src", "tests"]
exclude = ["**/.*", "**/__pycache__"]
extraPaths = ["../core/src"]
pythonVersion = "3.13"
venvPath = "../../"
venv = ".venv"
typeCheckingMode = "strict"
