[project]
name = "error"
version = "0.0.1"
readme = "README.md"
description = "Error"
requires-python = ">=3.14"
dependencies = [
  "core",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
